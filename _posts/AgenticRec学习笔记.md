核心思想：**通用 Agent 框架不解决搜广推的真问题**。它们解决的是「给 LLM装上工具去完成开放任务」，而搜广推的真问题是——
> 在严格的延迟约束下（端到端通常 < 200ms），如何把一个固定的 5 段管线变得「会反思、会换路、会兜底」，同时不破坏在线 A/B 的可观测性。
这不是把 LangGraph 套上去就能解决的。LangGraph 解决的是图执行，搜广推需要的是**带业务语义的元决策层**：什么场景下用哪一路召回、候选集多大就跳过粗排、什么样的分布偏置应该触发重跑。
核心设计：从 Pipeline 到 Council